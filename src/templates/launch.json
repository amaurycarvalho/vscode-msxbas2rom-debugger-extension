{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "msx",
      "request": "launch",
      "name": "Debug MSX BASIC",

      "program": "${file}",
      "rom": "${fileBasenameNoExtension}.rom",
      "cdb": "${fileBasenameNoExtension}.cdb",

      "openmsxPath": "${config:msxDebugger.openmsxPath}"
    }
  ]
}
