{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "msx",
      "request": "launch",
      "name": "Debug MSX BASIC",

      "program": "${file}",
      "rom": "${fileBasenameNoExtension}.rom",
      "cdb": "${fileBasenameNoExtension}.cdb",

      "enableDebugLogs": "${config:msxDebugger.enableDebugLogs}",
      "openmsxPath": "${config:msxDebugger.openmsxPath}",
      "preLaunchTask": "Build"
    }
  ]
}
